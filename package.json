{
  "name": "CrypToadz",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ CrypToadz",
    "create-local": "graph create --node http://localhost:8020/ CrypToadz",
    "remove-local": "graph remove --node http://localhost:8020/ CrypToadz",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 CrypToadz"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
